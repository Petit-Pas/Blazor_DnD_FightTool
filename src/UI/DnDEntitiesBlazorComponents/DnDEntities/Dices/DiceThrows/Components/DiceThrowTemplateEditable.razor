
@if (Template != null)
{
    <DiceThrowTemplateInput ValidateOnKeyPress="false" ValueExpression=@(() => Template) Value="Template" ValueChanged="(newTemplate) => OnTemplateChanged(newTemplate)" ValidationFor=@(() => Template)>
    </DiceThrowTemplateInput>
}
